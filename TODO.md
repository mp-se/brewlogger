# New features

## Lint

Check how R0801 can be handled (refactor needed), duplicated code

## System status
- Add option so that containers can register in redis so we can check if they are running or not (MDNS and BLE).

# Bugs

- TBD
 